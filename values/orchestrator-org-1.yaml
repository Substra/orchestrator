---
orchestrator:
  mode: standalone

postgresql:
  enabled: true

rabbitmq:
  auth:
    username: user
    password: password

ingress:
  enabled: true
  hosts:
  - host: orchestrator.node-1.com
    paths:
      - "/"
  tls:
    - hosts:
      - orchestrator.node-1.com
      secretName: orchestrator-tls
