---
orchestrator:
  mode: standalone

postgresql:
  enabled: true

rabbitmq:
  auth:
    username: user
    password: password

ingress:
  enabled: true
  hosts:
  - host: orchestrator.node-2.com
    paths:
      - "/"
  tls:
    - hosts:
      - orchestrator.node-2.com
      secretName: orchestrator-tls
