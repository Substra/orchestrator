---
orchestrator:
  mode: standalone

postgresql:
  enabled: true
  persistence:
    enabled: false

rabbitmq:
  auth:
    username: user
    password: password
  persistence:
    enabled: false

ingress:
  enabled: true
  hosts:
  - host: orchestrator.node-2.com
    paths:
      - "/"
  tls:
    - hosts:
      - orchestrator.node-2.com
      secretName: orchestrator-tls
