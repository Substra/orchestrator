name: Build and test

on:
  push:
    branches:
    - main
  pull_request:

env:
    GO111MODULE: on

jobs:
  build:
    name: Build and test
    runs-on: ubuntu-latest
    steps:
      - name: Check out source code
        uses: actions/checkout@v3
      - name: Install Go
        uses: actions/setup-go@v3
        with:
          go-version: 1.17.x
      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: ~/go/pkg/mod
          key: build-${{ hashFiles('**/go.sum') }}
      - name: Install protobuf codegen tool
        run: |
          curl -LO https://github.com/protocolbuffers/protobuf/releases/download/v3.14.0/protoc-3.14.0-linux-x86_64.zip
          unzip protoc-3.14.0-linux-x86_64.zip -d $HOME/.local
          echo "$HOME/.local/bin" >> $GITHUB_PATH
      - name: Install go tools
        # mockery v2.10.1 hangs
        # See: https://github.com/vektra/mockery/issues/442
        run: |
          cd /tmp
          go get google.golang.org/protobuf/cmd/protoc-gen-go
          go get google.golang.org/grpc/cmd/protoc-gen-go-grpc
          go get github.com/go-bindata/go-bindata/...
          go get github.com/vektra/mockery/v2@v2.10.0
      - name: Build
        run: make
      - name: Test
        run: make test
